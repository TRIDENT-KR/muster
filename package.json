{
  "name": "@musterhq/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Org-wide control plane for AI coding agent configuration — sync AGENTS.md, CLAUDE.md, skills, and MCP config from one source of truth.",
  "type": "module",
  "bin": {
    "muster": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
